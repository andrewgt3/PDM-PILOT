[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    integration: requires full docker stack (run with: pytest -m integration)
addopts = --ignore=notebooks
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
