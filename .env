# TimescaleDB / Postgres
DT_POSTGRES_USER=postgres
DT_POSTGRES_PASSWORD=password
DT_POSTGRES_DB=pdm_timeseries

# MongoDB
MONGO_USER=admin
MONGO_PASSWORD=password

# Redis
# (Redis setup here doesn't use auth by default in the compose, but good to have ready)
REDIS_HOST=localhost
REDIS_PORT=6379
